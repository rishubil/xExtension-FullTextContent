#!/bin/sh
# Stub obscura binary: returns sample HTML regardless of URL.
# Usage: obscura fetch <url> --dump html
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
if [ "$1" = "fetch" ]; then
    cat "${SCRIPT_DIR}/sample.html"
    exit 0
fi
printf 'stub-obscura: unknown command: %s\n' "$1" >&2
exit 1
